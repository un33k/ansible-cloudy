#!/usr/bin/env python3
"""
Claudia (Claude-inspired Infrastructure Assistant)
Intelligent Infrastructure Management Made Intuitive

Main executable script for Claudia CLI
"""

import sys
import os
from pathlib import Path

# Add the claudia module to the Python path
script_dir = Path(__file__).parent
claudia_dir = script_dir / "dev" / "claudia"
sys.path.insert(0, str(claudia_dir))

# Import and run main CLI
from cli.main import main

if __name__ == "__main__":
    main()