# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]

# C extensions
*.so

# Distribution / packaging
.Python
.mypy_cache/
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
*.egg-info/
.installed.cfg
*.egg

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*,cover

# Translations
*.mo
*.pot

# Django stuff:
*.log

# Sphinx documentation
docs/_build/

# PyBuilder
target/

cfg.txt

*history.txt

# virtual environment
.venv/

.cloudy
.cloudy.*

cloudy-old/

.DS_Store
.python-version

# Vault password files (security)
.vault_pass*
*vault_pass*
*.ansible-vault-pass*

# User's real vault files (examples are safe to commit)
*.vault.yml
*-vault.yml
.secrets/*.local.yml
.secrets/dev.yml
.secrets/ci.yml
.secrets/prod.yml

# Simple vault files (new system - examples are safe to commit)
.vault/*.yml
!.vault/*.example
!.vault/README.md

# Unencrypted secrets (if any)
.secrets/**/unencrypted_*