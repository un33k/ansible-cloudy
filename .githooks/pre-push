#!/bin/bash

# Smart pre-push hook: Auto-encrypt secrets before push, then decrypt after
# This allows fast local commits while ensuring only encrypted secrets reach remote

set -e

# Colors for output
RED='\033[0;31m'
YELLOW='\033[1;33m'
GREEN='\033[0;32m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

echo -e "${BLUE}🔒 Smart pre-push: Auto-encrypting secrets...${NC}"

# Function to check if a file is encrypted with ansible-vault
is_vault_encrypted() {
    local file="$1"
    if [[ -f "$file" ]]; then
        if head -n 1 "$file" | grep -q '^\$ANSIBLE_VAULT'; then
            return 0  # File is encrypted
        else
            return 1  # File is not encrypted
        fi
    fi
    return 1  # File doesn't exist
}

# Function to encrypt vault files
encrypt_vault_files() {
    local encrypted_files=()
    
    # Find all .secrets files that need encryption
    for vault_file in .secrets/*.yml; do
        if [[ -f "$vault_file" ]] && [[ "$vault_file" != *.template ]]; then
            if ! is_vault_encrypted "$vault_file"; then
                echo -e "${YELLOW}📝 Encrypting: $vault_file${NC}"
                
                # Activate virtual environment if it exists
                if [[ -f ".venv/bin/activate" ]]; then
                    source .venv/bin/activate
                fi
                
                # Check for ANSIBLE_VAULT_PASSWORD_FILE first
                if [[ -n "$ANSIBLE_VAULT_PASSWORD_FILE" ]] && [[ -f "$ANSIBLE_VAULT_PASSWORD_FILE" ]]; then
                    # Try encryption with password file and capture result
                    if ! ansible-vault encrypt "$vault_file" --vault-password-file="$ANSIBLE_VAULT_PASSWORD_FILE"; then
                        echo -e "${RED}❌ Failed to encrypt $vault_file with password file${NC}"
                        echo -e "${YELLOW}   Check your vault password file: $ANSIBLE_VAULT_PASSWORD_FILE${NC}"
                        exit 1
                    fi
                else
                    # Warn about missing ANSIBLE_VAULT_PASSWORD_FILE
                    echo -e "${YELLOW}⚠️  ANSIBLE_VAULT_PASSWORD_FILE is not set or file doesn't exist${NC}"
                    if [[ -n "$ANSIBLE_VAULT_PASSWORD_FILE" ]]; then
                        echo -e "${RED}   File not found: $ANSIBLE_VAULT_PASSWORD_FILE${NC}"
                    else
                        echo -e "${BLUE}   To avoid manual password entry, set:${NC}"
                        echo -e "${GREEN}   export ANSIBLE_VAULT_PASSWORD_FILE=~/.ansible-vault-pass${NC}"
                    fi
                    echo -e "${YELLOW}   Please enter vault password to encrypt $vault_file:${NC}"
                    
                    # Try manual encryption and capture result
                    if ! ansible-vault encrypt "$vault_file"; then
                        echo -e "${RED}❌ Failed to encrypt $vault_file${NC}"
                        exit 1
                    fi
                fi
                
                encrypted_files+=("$vault_file")
                echo -e "${GREEN}✅ Encrypted: $vault_file${NC}"
            fi
        fi
    done
    
    # If we encrypted any files, commit them
    if [[ ${#encrypted_files[@]} -gt 0 ]]; then
        echo -e "${BLUE}📦 Committing encrypted vault files...${NC}"
        git add "${encrypted_files[@]}"
        git commit -m "🔒 Auto-encrypt vault files before push

🤖 Generated with [Claude Code](https://claude.ai/code)

Co-Authored-By: Claude <noreply@anthropic.com>"
        
        # Store the list of files we encrypted for later decryption
        printf '%s\n' "${encrypted_files[@]}" > .git/encrypted_files.tmp
        echo -e "${GREEN}✅ Committed encrypted files${NC}"
        return 0
    else
        echo -e "${GREEN}✅ All vault files already encrypted${NC}"
        return 1  # No files were encrypted
    fi
}

# Function to decrypt vault files after push
decrypt_vault_files() {
    if [[ -f .git/encrypted_files.tmp ]]; then
        echo -e "${BLUE}🔓 Decrypting vault files for local development...${NC}"
        
        while IFS= read -r vault_file; do
            if [[ -f "$vault_file" ]] && is_vault_encrypted "$vault_file"; then
                echo -e "${YELLOW}🔓 Decrypting: $vault_file${NC}"
                
                # Activate virtual environment if it exists
                if [[ -f ".venv/bin/activate" ]]; then
                    source .venv/bin/activate
                fi
                
                # Check for ANSIBLE_VAULT_PASSWORD_FILE first
                if [[ -n "$ANSIBLE_VAULT_PASSWORD_FILE" ]] && [[ -f "$ANSIBLE_VAULT_PASSWORD_FILE" ]]; then
                    # Try decryption with password file and capture result
                    if ! ansible-vault decrypt "$vault_file" --vault-password-file="$ANSIBLE_VAULT_PASSWORD_FILE"; then
                        echo -e "${RED}❌ Failed to decrypt $vault_file with password file${NC}"
                        echo -e "${YELLOW}   Check your vault password file: $ANSIBLE_VAULT_PASSWORD_FILE${NC}"
                        echo -e "${RED}   Leaving file encrypted to prevent data loss${NC}"
                        continue
                    fi
                else
                    # Warn about missing ANSIBLE_VAULT_PASSWORD_FILE
                    echo -e "${YELLOW}⚠️  ANSIBLE_VAULT_PASSWORD_FILE is not set or file doesn't exist${NC}"
                    if [[ -n "$ANSIBLE_VAULT_PASSWORD_FILE" ]]; then
                        echo -e "${RED}   File not found: $ANSIBLE_VAULT_PASSWORD_FILE${NC}"
                    else
                        echo -e "${BLUE}   To avoid manual password entry, set:${NC}"
                        echo -e "${GREEN}   export ANSIBLE_VAULT_PASSWORD_FILE=~/.ansible-vault-pass${NC}"
                    fi
                    echo -e "${YELLOW}   Please enter vault password to decrypt $vault_file:${NC}"
                    
                    # Try manual decryption and capture result
                    if ! ansible-vault decrypt "$vault_file"; then
                        echo -e "${RED}❌ Failed to decrypt $vault_file${NC}"
                        echo -e "${RED}   Leaving file encrypted to prevent data loss${NC}"
                        continue
                    fi
                fi
                
                echo -e "${GREEN}✅ Decrypted: $vault_file${NC}"
            fi
        done < .git/encrypted_files.tmp
        
        # Clean up temp file
        rm .git/encrypted_files.tmp
        echo -e "${GREEN}✅ All files decrypted for local development${NC}"
    fi
}

# Function to check for other security violations
check_security_violations() {
    local violations=()
    
    # Check for private keys
    local private_keys=$(git ls-files | grep -E 'id_(rsa|dsa|ecdsa|ed25519)$' | grep -v '\.pub$')
    while IFS= read -r key_file; do
        if [[ -n "$key_file" ]]; then
            violations+=("❌ Private SSH key in repository: $key_file")
        fi
    done <<< "$private_keys"
    
    # Note: We skip checking non-vault files for secrets since we have a vault system
    # and Ansible templates use variables like {{ vault_password }} which are safe
    
    if [[ ${#violations[@]} -gt 0 ]]; then
        printf '%s\n' "${violations[@]}"
        return 1
    fi
    
    return 0
}

# Main execution
echo -e "${BLUE}🔍 Checking for security violations...${NC}"

# Check for non-vault security issues
if ! check_security_violations; then
    echo -e "\n${RED}🚨 PUSH BLOCKED: Security violations detected!${NC}"
    echo -e "${YELLOW}Please fix the above issues before pushing.${NC}"
    exit 1
fi

# Encrypt vault files if needed
files_encrypted=false
if encrypt_vault_files; then
    files_encrypted=true
fi

# Create a trap to decrypt files even if push fails
trap 'if [[ "$files_encrypted" == true ]]; then decrypt_vault_files; fi' EXIT

echo -e "\n${GREEN}🚀 Security checks passed! Proceeding with push...${NC}"

# Note: The actual push happens after this script
# The trap will decrypt files after push completes (or fails)